second commit and make branch and checkout to origin develop branch

git checkout -b develop
=> make develop branch and checkout to develop branch at the same time.

git add README.md
=> staging README.md file to develop branch.

git commit -m 'commit message'
=> commit on develop branch with 'commit message'

git push origin develop
=> pushing to origin's develop branch.